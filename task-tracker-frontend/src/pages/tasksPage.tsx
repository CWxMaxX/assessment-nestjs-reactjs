import React from "react";

import DefaultLayout from "@/layouts/default";

const TasksPage: React.FC = () => {
  return (
    <DefaultLayout>
      <div>
        <h1>Tasks Page</h1>
        <p>This is a sample component for the tasks page.</p>
      </div>
    </DefaultLayout>
  );
};

export default TasksPage;
